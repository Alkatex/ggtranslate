{
    "name": "ggtranslate",
    "version": "1.0.0",
    "private": true,
    "description": "Real-time gaming voice translation desktop app",
    "engines": {
      "node": ">=20.0.0",
      "pnpm": ">=9.0.0"
    },
    "scripts": {
      "dev": "pnpm --filter desktop dev",
      "build": "pnpm --filter desktop build",
      "lint": "pnpm -r lint",
      "type-check": "pnpm -r type-check"
    },
    "devDependencies": {
      "typescript": "^5.5.0",
      "@types/node": "^20.0.0",
      "prettier": "^3.3.0"
    }
  }
  